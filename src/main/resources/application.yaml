server:
  port: 8080
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/ecommerce"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  output:
    ansi:
      enabled: always

  mail:
    host: "smtp.gmail.com"
    port: 587
    username: "dinhtruonghioo4@gmail.com"
    password: "lcjo kgdj zulj vzgb"
    properties.mail.smtp:
      auth: true
      starttls.enable: true
